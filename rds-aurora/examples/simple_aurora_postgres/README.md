# Simple AWS Aurora Postgres Cluster

This example represents an Postgres Aurora Stack, which supports alerting and password rotation.

## Prerequisites

- AWS account
- VPC and private/public subnets

## Usage

```bash
$ terraform init
$ terraform plan
$ terraform apply
```
